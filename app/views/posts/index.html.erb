<h1>Disaster Information's</h1>

<table class="table table-bordered table-responsive">
  <thead class="bg-info">
  <tr>
    <th>ID</th>
    <th>Title</th>
    <th>Content</th>
    <th>Description</th>
  </tr>
  </thead>
  <tbody>
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.id %></td>
      <td><%= post.title %></td>
      <td><%= post.content %></td>
      <td><%= post.description %></td>
    </tr>
  <% end %>
  </tbody>
</table>
  